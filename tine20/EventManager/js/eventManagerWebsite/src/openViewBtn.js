/*Tine.Tinebase.appMgr.isInitialised('EventManager').then(() => {
    const app = Tine.Tinebase.appMgr.get('EventManager');
    const openWebsiteBtn = Ext.extend(Ext.Button, {
        text: app.i18n._('Website'),
        scale: 'medium',
        minWidth: 60,
        rowspan: 2,
        iconAlign: 'top',
        requiredGrant: 'readGrant',
        iconCls: 'cal-floorplan-view-type',
        handler: () => {
            window.open(Tine.Tinebase.common.getUrl() + '/EventManager/view/event/', '_blank');
        }
    });

    Ext.ux.ItemRegistry.registerItem('EventManager-MainScreenPanel-ViewBtnGrp', openWebsiteBtn, 40);
});*/
