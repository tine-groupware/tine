{#
avialable parameters:
    link
    app.branding.title
    contact
    event
#}

{% extends 'Tinebase/views/emails/base.html.twig' %}

{% block subject %}
    {{ _('Manage your registration') }}
{% endblock %}

{% block body %}
    <div class="mt-3 mb-5">
        <p class="text-start">{{ _('To manage your registration, please follow the link below') }}</p>
    </div>
    <div class="ax-center my-5">
        <a class="btn btn-primary p-2" href="{{ link }}" role="button">{{ _('Manage my registration') }}</a>
    </div>
{% endblock %}