{#
avialable parameters:
    link
    app.branding.title
    contact
#}

{% block subject %}
    {% if dipr != '' %}
        {{ 'Confirm your E-mail address' }}
    {% else -%}
        {{ 'Confirm your registration' }}
    {% endif %}
{% endblock %}

{% block body %}
    {{ 'Hello ' }}  {{ contact.getTitle() | default('user') }}

    {{ 'thank you for your registration, to complete the registration, please confirm your e-mail address via this link :' }}

    {{ link }}

    {{ 'Thank you.' }}
    {{ 'Your %1$s Team' | format(app.branding.title) }}
{% endblock %}