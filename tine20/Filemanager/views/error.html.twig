{% extends 'Filemanager/views/base.html.twig' %}

{% block title %}
    <title>{{ title | default(_('File not found!')) }}</title>
{% endblock %}

{% block content %}
    <div class="empty-state">
        <i class="fas"></i>
        <h4>{{ content | default(_('File not found!')) }}</h4>
    </div>
{% endblock %}